#MODULES AND PIP

#Un módulo es un archivo de python que podemos importar en nuestro archivo de python actual, así puedo acceder a toda la
#información de ese archivo desde el que estoy trabajando.
#Pones "import" y su nombre.

import useful_tools

print(useful_tools.roll_dice(10))

#En la página de modules list de python tienes mazo módulos para usar. Puedes buscar módulos específicos de terceros.
#Hay built in y external modules. Si te fijas en la izquierda, recoges la carpeta en la que estás o te vas abajo del t0d0,
#puedes ver que hay una carpeta que se llama "External Libraries". Ahí dentro está la librería de external modules, en Lib.
#Puedes instalarte los módulos de terceros para tenerlos en tu librería. Se hace con "pip". Es un package manager.
#Abres la terminal y pones "pip install python_docx". Se te guarda en la carpeta docx de Python.






